# 统一版本验收记录 · 2026-09-12

## 版本与范围

本记录对应统一源码 **`7ee8209`**，覆盖前端动效、静态场景刷新、Agent 发育呈现、功耗展示及 Windows 原生构建与启动冒烟。

- `a4608e4`：冻结后的环境动效与胚胎/Agent 发育改动，通过 `1283ff8` 合入。
- `4c4b166`：原生功耗采样与前端展示，通过 `3c2959c` 合入。
- `7ee8209`：修复暂停或减少动效时，搜索、选择与调整尺寸后的静态场景被旧主题快照遮挡的问题；相关 **7 项回归测试先失败、修复后通过**。

本次记录整合主任务的全量验证、浏览器实测、构建和启动冒烟结果，以及独立执行的 Rust 验证结果。编写本记录时未重复测试或修改实现。

## 自动验证与构建

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| `npm run verify` | 通过 | TypeScript 检查、**28 个测试文件 / 156 项测试**及生产前端构建全部成功。 |
| `cargo test --lib --locked --offline` | 通过 | **13 项测试**全部成功，包含共享采样状态、真实系统采样、Windows 图标、功耗字段序列化、能量计数差分及 Sysman ABI 等检查。 |
| `cargo check --locked --offline` | 通过 | Rust 检查无警告、无错误；使用现有工具链及缓存，临时 Unicode 安全驱动器映射已清理。 |
| Tauri 全量构建 | 通过 | `7ee8209` 的 MSI 与 NSIS 安装包构建成功，产物已复制到 `release/`。 |

Rust 独立验证耗时：测试 105.53 秒、检查 28.56 秒，共 134.10 秒。Tauri 打包阶段仍出现既有 GNU linker 多 manifest 警告；该警告与上述无警告的 Rust test/check 阶段分别记录。

## 浏览器实测

在 **1040 × 720** 视口完成以下检查：

- 两种主题均正常呈现；暂停状态下，搜索得到零匹配结果时显示 **0 个生物**，没有旧画面或残影覆盖。清除搜索后场景恢复。
- 暂停搜索的旧主题快照问题已在浏览器复查；暂停及减少动效下的选择、尺寸变化等额外场景由上述回归测试覆盖。
- 中英文功耗演示标签与说明正常；Agent 发育提示明确说明其依据为年龄，不能作为真实任务进度。
- 进入壁纸模式后，稳定状态下 Canvas 覆盖完整 **1040 × 720** 视口，HUD 正常显示；Escape 可返回窗口模式。
- 初入壁纸模式的瞬间截图出现过较小视口，属于尺寸过渡观察；稳定后的视口尺寸与画面正确，未将过渡帧当作稳定布局结果。

## Windows 原生启动冒烟

对复制至 `release/` 的统一版本进行了 **40 秒**启动冒烟。第 **8、16、24、32、40 秒**检查均为：

- 进程存活（`Alive = true`）。
- 主窗口响应正常（`Responding = true`）。
- 主窗口句柄非零。

冒烟脚本成功结束，退出码为 **0**。检查结束后仅停止本次冒烟自行启动的进程；未将脚本退出码表述为应用自行退出结果。

## 验证边界

- 浏览器演示数据用于界面与交互验证，不能证明硬件功耗采样有效。真实功耗能力以功耗任务提供的独立原生证据为准；不同传感器的测量范围仍需按来源说明理解。
- 尚未验证 Windows **WorkerW 桌面嵌入**或在干净环境中执行安装。MSI/NSIS 构建成功与窗口启动响应正常，不等同于这两项已验收。
- 首次资源解码 **103.9 ms** 峰值来自上游记录，本次未重新测量该指标；没有作出稳定 **120 FPS** 的承诺。
- Agent 发育阶段是**年龄代理**，不是任务完成度、执行步骤或剩余工作量的测量。

在以上范围内，统一版本的自动验证、浏览器交互、Windows 构建和 40 秒原生响应冒烟均已通过。
