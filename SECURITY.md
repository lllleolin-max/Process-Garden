# Security policy

Report vulnerabilities privately to the project maintainer rather than opening a public issue with exploit details.

The highest-risk surfaces are custom ZIP parsing, CSS token validation, Tauri command permissions and Windows wallpaper recovery. Reproduction reports should include the app version, Windows version, package manifest if relevant, and whether the behavior occurs in browser Demo mode or the native build. Never attach sensitive process dumps or file contents.
