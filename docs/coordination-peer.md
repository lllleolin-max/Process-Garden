# Asset and lifecycle handoff

Updated: 2026-09-12

## Repository and ownership

- Private repository: https://github.com/lllleolin-max/Process-Garden
- Shared source baseline: `75cdf8a` on `main`. This captures both tasks' in-progress improvements, not a verified pre-change release.
- Integration owner: task `01a09449-4661-7ca1-8ffa-0b8c94ddb736`, branch `feat/motion-integration`, original Process Garden directory. Owns frontend reconciliation, full verification and packaging.
- Asset/lifecycle contributor: task `019fe686-f462-7e11-8d6e-6d851752b6cc`, branch `docs/asset-motion-handoff`, independent worktree `../Process Garden.worktrees/asset-motion`. This PR changes documentation only.
- Contributor implementation writing in the original directory has stopped. Assets and lifecycle fixes below already exist in the baseline; do not replay them by cherry-pick.

The integration owner coordinates the remaining bounded agent edits. New concurrent assignments use separate worktrees and branches. No task switches another task's checkout, stages its files, overwrites its uncommitted work or force-pushes a shared branch. Source changes go through reviewed PRs; the integration owner alone generates final release artifacts after verification.

## Production assets

Both assets were created with built-in ImageGen. Only project artwork and visual descriptions were supplied; no live process data or credentials were sent.

| Asset | Use | SHA-256 |
| --- | --- | --- |
| `public/assets/generated/shared/celestial-atlas-v1.png` | Four equal quadrants: Garden sun, Garden moon, Eldritch sun, Eldritch moon. Preserve the original alpha when slicing. | `66BD72172D1500F19468F495CDF3E0863BC25942CF13C391D05D6AAD98DB3987` |
| `public/assets/generated/eldritch/cores/eldritch-core-maw-v3.png` | Generated open mouth, teeth, gums and saliva on solid black for screen compositing. Render the dark throat beneath it to occlude swallowed organisms. | `D2A3C674775EA3312F03595D21EEFF0FD82A23F222E5F27DC7964183C2A9B2CF` |

Prompt descriptions and generation corrections are recorded in [the asset log](../design/motion-assets-2026-09-12.md). `eldritch-core-maw-v2.png` is a rejected intermediate with an opaque checkerboard; it is excluded from source control and must not be packaged as production artwork.

## Contributor implementation already captured

- `src/animation/frameRate.ts`: early-vsync tolerance advances the consumed deadline, avoiding a second frame charged to the same interval.
- `src/animation/sceneClock.ts`: pause and hidden time do not advance visual lifecycle time; long frame gaps are bounded.
- `src/ecology/population.ts`: ranking hysteresis and stable visible ordering with selected-process retention.
- `src/animation/eldritchLifecycle.ts`: shockwave radius progresses independently of fading opacity; spiral motion starts with suction instead of anticipation.
- `src/components/GardenCanvas.tsx`: retain nodes across theme changes; consume the generated celestial atlas and maw; retain closing/recoil after organisms turn invisible; damp selection/hover emphasis; use stable PID-based cord curves; resolve actual Canvas font families; keep labels inside the viewport; replace per-node opacity filters with composited alpha.
- `src/animation/motionRegression.test.ts`: regression checks for pause/resume continuity, early-vsync pacing and shockwave progression after consumption.

Canvas also contains integrator/agent work. Review its final combined version as a whole. The original programmatic triangular-teeth fallback was still present at handoff and should be removed if superseded by the generated maw. The contributor's proposed `motionTicker.ts`, `AnimatedNumber.tsx` and alternate Sparkline rewrite did not land; preserve the actual integrator implementation.

## Verification evidence and remaining acceptance

The three contributor regression tests passed with `npm.cmd test -- src/animation/motionRegression.test.ts` before this documentation PR. This is not evidence of whole-application readiness.

The typecheck errors observed around 14:32 in SettingsDrawer and TopBar were from an in-progress display-mode contract change. The integration task owns resolving that contract and verifying the completed result. No frontend or release artifacts were regenerated by the contributor after coordination.

Before release, verify:

1. Both built-in themes load the intended generated assets; no opaque checkerboard, missing-image fallback or unreadable process identity.
2. Swallow sequence shows anticipation, suction, jaw occlusion, bite and complete recoil. Shockwave radius never contracts as opacity fades.
3. Pause/resume, hidden-tab recovery, search during pause, resize and theme switching remain continuous.
4. Reduced-motion preference suppresses strong spatial motion while preserving readable state changes. Exercise 30/60/120 Hz targets without claiming hardware can exceed its refresh rate.
5. Full `npm run verify`, the documented Rust checks when applicable, browser QA and native launch pass on the integrated commit.
6. Commit/tag and artifact hashes identify exactly which source produced the delivered installers. Generated binaries, dependency trees, local logs and environment files remain out of source commits.

The contributor left its local Vite server at `http://127.0.0.1:1420/` (session `97218`) for integration QA. That address is a local preview, not a published deployment.
